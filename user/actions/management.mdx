---
title: "Manage, edit & export actions"
description: "Edit, test, import, export, and delete your actions."
---

Once you have actions running on a schedule, the **Actions builder** is also where you maintain them. From the action list on the left, select any action to open it in the editor on the right, where you can change how it behaves, test it on demand, share it as a file, or remove it.

Actions are grouped into two sections: **Starter Actions** (the starter pack that ships with BunnyOS) and **Custom Actions** (the ones you create or import). Each row shows a status dot, how often it runs, how many tools it may use, and when it last ran.

<Note>
If you are new to actions, start with [Build an action](/user/actions/builder) to learn what each field does, then come back here to manage what you have.
</Note>

## Edit an action

Selecting an action opens the editor. You can change any of the following, then save:

| Field | What it controls |
| --- | --- |
| **name** | The label shown in your action list. |
| **every** | How often the action runs, picked from the dropdown. |
| **enabled / paused** | The toggle at the top right. Enabled means it runs on its schedule; paused means it stays defined but never runs automatically. |
| **instructions** | Plain-language instructions describing what to watch for, when to post an alert, and when to post a one-click recommendation. |
| **tools** | What the action is allowed to use. Toggle **all** on to let it use every available tool, or turn it off and pick specific tools. The counter shows how many are enabled. |

<Steps>
  <Step title="Open the action">
    Click an action in the list to load it into the editor.
  </Step>
  <Step title="Make your changes">
    Adjust the name, schedule, instructions, or tools.
  </Step>
  <Step title="Save">
    Click **save**. You will see a confirmation, and the list refreshes.
  </Step>
</Steps>

<Tip>
If the tools panel shows no tools available, enable some services first in [Configure, Services](/user/configure/services). Actions can only use tools you have turned on.
</Tip>

## Starter actions stay locked

Starter actions are protected: editing one saves your changes as a new custom copy rather than overwriting the original, which keeps its locked definition and cannot be deleted. See [Build an action](/user/actions/builder) for the full starter-lock rule.

## Test with Run now

Use **run now** to run an action once, on demand, without waiting for its schedule. See the Run now section of [Build an action](/user/actions/builder) for details.

## Delete a custom action

Custom actions have a **delete** button at the bottom of the editor. Deleting is permanent and removes the action from your list.

<Warning>
Deletion cannot be undone, and only custom actions can be deleted. Starter actions are protected and have no delete option.
</Warning>

## Export and import actions

Actions can be saved to and loaded from files for backup or sharing, and an imported file always becomes a new custom action. See the export/import details in [Build an action](/user/actions/builder).

## Related pages

<CardGroup cols={2}>
  <Card title="Build an action" icon="square-pen" href="/user/actions/builder">
    Create an action from scratch and learn what each field does.
  </Card>
  <Card title="Action inbox" icon="inbox" href="/user/actions/inbox">
    See the alerts and one-click recommendations your actions post.
  </Card>
  <Card title="Services" icon="plug" href="/user/configure/services">
    Enable the tools your actions are allowed to use.
  </Card>
  <Card title="Data & keys" icon="key" href="/user/getting-started/data-and-keys">
    Choose between BunnyDS and bring-your-own-key for the models that power your actions.
  </Card>
</CardGroup>
