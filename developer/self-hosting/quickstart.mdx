---
title: "Quickstart"
description: "Clone, install, configure, and run bunnyOS locally in a few commands."
---

bunnyOS is the action social network on Base, and the first open-source project built on top of [Base MCP](https://mcp.base.org). It is designed to be self-hosted: you run it, you own your keys, and you can audit the whole stack. This page gets a local instance running end to end.

The source lives at [github.com/bunnyos/base-agent](https://github.com/bunnyos/base-agent). By the end of these steps you will have the API server and the web interface running locally, talking to your own Postgres database.

<Note>
  bunnyOS holds **no private keys**. Your signing keys stay in your Base wallet, and the agent can only draft a transaction that *you* approve. Self-hosting does not change that model, see [Security](/developer/self-hosting/security).
</Note>

## Requirements

Make sure the following are installed and on your `PATH` before you start.

| Tool | Version | Notes |
|---|---|---|
| [Node.js](https://nodejs.org) | **24** | The monorepo targets Node 24. |
| [pnpm](https://pnpm.io) | latest | Used for the workspace install and per-package scripts. |
| [PostgreSQL](https://www.postgresql.org) | **16** | All persistent state lives here. |

<Tip>
  Check your versions with `node --version`, `pnpm --version`, and `postgres --version`. If you do not have pnpm yet, the quickest path is `corepack enable` (bundled with Node), or follow the [pnpm install guide](https://pnpm.io/installation).
</Tip>

## Run it

<Steps>
  <Step title="Clone and install">
    Clone the repository and install all workspace dependencies from the root.

    ```bash
    git clone https://github.com/bunnyos/base-agent
    cd base-agent
    pnpm install
    ```

    `pnpm install` resolves every package in the monorepo at once, the API server (`@workspace/api-server`), the interface (`@workspace/interface`), the database package (`@workspace/db`), and the shared libraries under `lib/*`.
  </Step>

  <Step title="Configure your environment">
    Create a `.env` file at the repo root and set the required values.

    ```bash
    touch .env
    ```

    These variables are required to boot:

    | Variable | Required | What it does |
    |---|---|---|
    | `DATABASE_URL` | yes | Postgres connection string for your local database. |
    | `SESSION_SECRET` | yes | A random string of **16 characters or more**. Set it once and do not rotate it casually. How it protects sessions and data at rest is covered in [Security](/developer/self-hosting/security). |
    | `PORT` | yes | The port each dev server binds to (the API server and the interface each read it). Use `5000` for the API server and `5173` for the interface, as in the commands below. |
    | `BASE_PATH` | yes | The base path the interface is served under. Use `/` for local dev. |

    <Warning>
      Set `SESSION_SECRET` to a 16+ character random value once, and do not rotate it casually. Full environment reference in [Configuration](/developer/self-hosting/configuration).
    </Warning>
  </Step>

  <Step title="Push the database schema">
    With `DATABASE_URL` pointing at your Postgres 16 instance, push the schema into it.

    ```bash
    pnpm --filter @workspace/db run push
    ```

    This applies the Drizzle schema from `@workspace/db` directly to your database, no migration files to manage for local development.
  </Step>

  <Step title="Start the two dev servers">
    bunnyOS runs as two processes. Each reads `PORT`, so set it inline per terminal. Open **two separate terminals** from the repo root.

    ```bash
    # Terminal 1: API server on :5000
    PORT=5000 pnpm --filter @workspace/api-server run dev
    ```

    ```bash
    # Terminal 2: interface on :5173
    PORT=5173 pnpm --filter @workspace/interface run dev
    ```

    The interface calls the API server cross-origin (the server allows it via CORS), so both processes need to be running for the app to work.
  </Step>

  <Step title="Open the UI">
    Visit the interface in any browser:

    ```text
    http://localhost:5173
    ```

    You should see the bunnyOS interface. From here you can connect your Base wallet, browse and trade bunny keys on bunnyDEX, and follow the recommendations that gather in your Actions inbox.
  </Step>
</Steps>

## What you just started

For how the systems compose and what running bunnyOS yourself involves, see the [self-hosting overview](/developer/self-hosting/overview). For which tool sources need a per-user key, see the key-required matrix in [Configuration](/developer/self-hosting/configuration).

## Next steps

<CardGroup cols={2}>
  <Card title="Self-hosting overview" icon="server" href="/developer/self-hosting/overview">
    How the pieces fit together and what running bunnyOS yourself involves.
  </Card>
  <Card title="Configuration" icon="sliders-horizontal" href="/developer/self-hosting/configuration">
    Every environment variable, protocol toggle, and per-user key explained.
  </Card>
  <Card title="Security model" icon="shield-check" href="/developer/self-hosting/security">
    No private keys on the system, at-rest encryption, and signed sessions.
  </Card>
</CardGroup>

<Note>
  bunnyOS is experimental software, licensed under **AGPL-3.0**. Always review permissions and understand transaction risks before interacting with onchain systems, you are responsible for your own wallets and approvals. Report security issues privately to **security@bunnyos.ai**, never in a public issue.
</Note>
